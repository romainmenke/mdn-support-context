document.addEventListener("DOMContentLoaded",()=>{chrome.storage.sync.get(e=>{if(!e||!e.browserslist){document.getElementById("browserslist").value="defaults";return}document.getElementById("browserslist").value=e.browserslist})}),document.getElementById("save").addEventListener("click",()=>{let e=document.getElementById("browserslist").value.trim();e||(e="defaults"),chrome.storage.sync.set({browserslist:e},()=>{// Update status to let user know options were saved.
let e=document.getElementById("status");e.textContent="Options saved.",setTimeout(()=>{e.textContent=""},750)})});